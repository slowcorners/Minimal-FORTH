
10 VARIABLE SIZE

0 VARIABLE CNT 202 ALLOT

: SCALE >R -1 0 R> U/ SWAP DROP 1+ ;

: RAN   SCALE >R RND 0 R> U/ SWAP DROP ;

: RT    0 DO SIZE @ RAN 1 << CNT + 1 SWAP +! LOOP ;

: CLR   CNT 202 ERASE ;

CLR

: PRETTY
  CR
  SIZE @ 0
  DO
    I DUP 3 .R BL EMIT 1 << CNT + @ 10 / 0
    DO
      ASCII * EMIT
    LOOP
    CR
  LOOP
  CR
;
